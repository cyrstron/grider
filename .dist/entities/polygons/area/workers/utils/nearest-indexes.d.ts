import { GridParams } from '../../../../grid-params';
export declare function calcNearestIndexes(i: number, j: number, params: GridParams): number[][];
export declare function calcNearestAndTouchedIndexes(i: number, j: number, params: GridParams): number[][];

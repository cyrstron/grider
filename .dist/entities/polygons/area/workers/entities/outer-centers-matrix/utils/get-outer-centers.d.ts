import { CenterPoint } from '../../../../../../points/center-point';
import { CentersMatrix } from '../../centers-matrix';
export declare function getOuterCentersMatrix(matrix: CentersMatrix): Array<Array<CenterPoint | 'outer' | undefined>>;

import { GeoPoint } from '../../../../../../points/geo-point';
import { PolyMatrix } from '../poly-matrix';
export declare function callNextIndexes(matrix: PolyMatrix, points: GeoPoint[], startI: number, startJ: number, prevI?: number, prevJ?: number): [number, number];

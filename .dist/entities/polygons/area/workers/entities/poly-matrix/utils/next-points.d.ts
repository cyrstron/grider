import { GeoPoint } from '../../../../../../points/geo-point';
import { PolyMatrix } from '../poly-matrix';
export declare function getNextPoints(matrix: PolyMatrix, points: GeoPoint[], outerI: number, outerJ: number, isInner?: boolean): GeoPoint[];

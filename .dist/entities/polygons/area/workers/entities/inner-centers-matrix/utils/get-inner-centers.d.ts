import { CenterPoint } from '../../../../../../points/center-point';
import { CentersMatrix } from '../../centers-matrix';
export declare function getInnerCentersMatrix(matrix: CentersMatrix, innerEmpties: number[][]): Array<Array<CenterPoint | undefined | 'inner'>>;
